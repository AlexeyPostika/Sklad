<UserControl x:Class="GMaps_Test.FlexControls.FlexListView.FlexListViewItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GMaps_Test.FlexControls.FlexListView"
             mc:Ignorable="d" 
             x:Name="control">
    <Grid>
        <ListView x:Name="list" ItemsSource="{Binding ElementName=control, Path=ListData, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"                                
                  Mouse.MouseUp="list_MouseUp"
                  MaxWidth="450">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding display_name}" Header="Name" Width="360"/>
                    <GridViewColumn Header="Icon" Width="35">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding icon}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>


                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
