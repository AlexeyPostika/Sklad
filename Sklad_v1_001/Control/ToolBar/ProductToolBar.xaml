<UserControl x:Class="Sklad_v1_001.Control.ToolBar.ProductToolBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sklad_v1_001.Control.ToolBar"
             xmlns:res="clr-namespace:Sklad_v1_001.Properties"
             mc:Ignorable="d"
             x:Name="control"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid  Style="{DynamicResource Grid}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="Auto" MaxWidth="80"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="Auto" MaxWidth="80"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="Auto" MaxWidth="80"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="Auto" MaxWidth="80"/>
            <ColumnDefinition Width="5"/>
        </Grid.ColumnDefinitions>

        <Button x:Name="ApplyButton" Grid.Column="1"
                HorizontalAlignment="Left" Width="auto"                
                Click="ApplyButton_Click"
                IsEnabled="{Binding ElementName=control, Path=IsEnableAdd, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
            <StackPanel Width="Auto" Orientation="Horizontal" HorizontalAlignment="Center">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="24"/>
                    </Grid.RowDefinitions>
                    <Image Source="{Binding ElementName=control, Path=EditImage, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" StretchDirection="DownOnly" Stretch="UniformToFill" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                    <TextBlock Grid.Row="1" Text="{x:Static res:Resources.EDITBUTTON}" Width="Auto" HorizontalAlignment="Stretch" />
                </Grid>
            </StackPanel>
        </Button>

        <Button x:Name="SaveButton" Grid.Column="3"
                HorizontalAlignment="Left" Width="auto"                
                Click="SaveButton_Click"
                IsEnabled="{Binding ElementName=control, Path=IsEnableAdd, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
            <StackPanel Width="Auto" Orientation="Horizontal" HorizontalAlignment="Center">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="24"/>
                    </Grid.RowDefinitions>
                    <Image Source="{Binding ElementName=control, Path=FilterImage, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"  HorizontalAlignment="Center" StretchDirection="DownOnly" Stretch="UniformToFill"/>
                    <TextBlock Grid.Row="1" Text="{x:Static res:Resources.FILTER}" Width="Auto" HorizontalAlignment="Center" />
                </Grid>
            </StackPanel>
        </Button>

        <Button x:Name="EditButton" Grid.Column="5"
                HorizontalAlignment="Left" Width="auto"                
                Click="EditButton_Click"
                IsEnabled="{Binding ElementName=control, Path=IsEnableAdd, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
            <StackPanel Width="Auto" Orientation="Horizontal" HorizontalAlignment="Center">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="24"/>
                    </Grid.RowDefinitions>
                    <Image Source="/Sklad_v1_001;component/Icone/IconEdit.png"  HorizontalAlignment="Right" StretchDirection="DownOnly" Stretch="UniformToFill" Margin="10,0,10,0"/>
                    <TextBlock Grid.Row="1" Text="{x:Static res:Resources.EDITBUTTON}" Width="Auto" HorizontalAlignment="Center" />
                </Grid>
            </StackPanel>
        </Button>

        <Button x:Name="ClearButton" Grid.Column="7"
                HorizontalAlignment="Left" Width="auto"                
                Click="ClearButton_Click"
                IsEnabled="{Binding ElementName=control, Path=IsEnableAdd, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
            <StackPanel Width="Auto" Orientation="Horizontal" HorizontalAlignment="Center">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="24"/>
                    </Grid.RowDefinitions>
                    <Image Source="/Sklad_v1_001;component/Icone/IconClearAllFilters_X32.png"  HorizontalAlignment="Right" StretchDirection="DownOnly" Stretch="UniformToFill" Margin="10,0,10,0"/>
                    <TextBlock Grid.Row="1" Text="{x:Static res:Resources.CLEAR}" Width="Auto" HorizontalAlignment="Center" />
                </Grid>
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
